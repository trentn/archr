import logging

l = logging.getLogger("archr.arsenal.fuzztainer")

from . import Bow

class FuzzTainerBow(Bow):
    """
    Fuzzing a target in the form of a Docker image.
    """

    def __init__(self, target):

    def nock(self):

    def fire(self):
